spring:
  liquibase.enabled: true
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:db
    username: sa
    password: sa
  jpa:
    show-sql: true
logging.level.org.hibernate: info

